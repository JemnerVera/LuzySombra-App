# Google Sheets Configuration
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_SHEETS_SHEET_NAME=Data-app
GOOGLE_SHEETS_CREDENTIALS_BASE64=your_credentials_base64_here
GOOGLE_SHEETS_TOKEN_BASE64=your_token_base64_here

# API Configuration
# Dejar vacío para usar URLs relativas (funciona en cualquier puerto)
# Solo configurar si necesitas una URL absoluta específica
NEXT_PUBLIC_API_URL=

# SQL Server Configuration
# IMPORTANTE: Configurar estas variables en .env.local (NO commitear .env.local)
SQL_SERVER=your_server_ip_or_hostname
SQL_DATABASE=your_database_name
SQL_PORT=1433
SQL_ENCRYPT=true

# SQL Server Authentication
# Para servidor AgroMigiva: usar ucown_powerbi_desa para escritura, ucser_powerbi_desa para lectura
SQL_USER=your_sql_user
SQL_PASSWORD=your_sql_password

# Data Source Configuration
# Options: 'sql' (only SQL Server), 'sheets' (only Google Sheets), 'hybrid' (both, SQL primary)
DATA_SOURCE=sql

# Resend API Configuration
# API Key de Resend (obtener desde https://resend.com/api-keys)
RESEND_API_KEY=re_tu_api_key_aqui
# Email desde el cual se envían las alertas (debe estar verificado en Resend)
# Dominio: updates.agricolaandrea.com
RESEND_FROM_EMAIL=no-reply@updates.agricolaandrea.com
# Nombre que aparece como remitente
RESEND_FROM_NAME=Sistema de Alertas LuzSombra

# Destinatarios de Alertas (JSON array) - FALLBACK si no hay contactos en image.Contacto
# NOTA: Si hay contactos en image.Contacto, se usan esos. Esta variable solo se usa como fallback.
ALERTAS_EMAIL_DESTINATARIOS=["admin@example.com", "agronomo@example.com"]
ALERTAS_EMAIL_CC=["manager@example.com"]
# ALERTAS_EMAIL_BCC=["backup@example.com"]

# JWT Authentication (para integración con AgriQR)
# Secret key para firmar tokens JWT - CAMBIAR EN PRODUCCIÓN
JWT_SECRET=your-super-secret-key-change-in-production

# NOTA: Las API Keys ahora se guardan en SQL Server (tabla image.Dispositivo)
# Ya NO es necesario configurar VALID_API_KEYS aquí
# Las apiKeys se gestionan desde la base de datos

# Development
NODE_ENV=development
