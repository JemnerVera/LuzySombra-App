# SQL Server Configuration
# IMPORTANTE: Configurar estas variables en .env.local (NO commitear .env.local)
SQL_SERVER=your_server_ip_or_hostname
SQL_DATABASE=your_database_name
SQL_PORT=1433
SQL_ENCRYPT=true

# SQL Server Authentication
# Usuario para aplicación LuzSombra (DESA y PROD)
# Usuario: ucser_luzsombra_desa (DESA) / ucser_luzSombra (PROD)
SQL_USER=ucser_luzsombra_desa
SQL_PASSWORD=D3s4S3r12

# Data Source Configuration
# Options: 'sql' (only SQL Server)
DATA_SOURCE=sql

# Resend API Configuration
# API Key de Resend (obtener desde https://resend.com/api-keys)
RESEND_API_KEY=re_tu_api_key_aqui
# Email desde el cual se envían las alertas (debe estar verificado en Resend)
# Dominio: updates.agricolaandrea.com
RESEND_FROM_EMAIL=no-reply@updates.agricolaandrea.com
# Nombre que aparece como remitente
RESEND_FROM_NAME=Sistema de Alertas LuzSombra

# Destinatarios de Alertas (JSON array) - FALLBACK si no hay contactos en evalImagen.Contacto
# NOTA: Si hay contactos en evalImagen.Contacto, se usan esos. Esta variable solo se usa como fallback.
ALERTAS_EMAIL_DESTINATARIOS=["admin@example.com", "agronomo@example.com"]
ALERTAS_EMAIL_CC=["manager@example.com"]
# ALERTAS_EMAIL_BCC=["backup@example.com"]

# JWT Authentication (para integración con AgriQR)
# Secret key para firmar tokens JWT - CAMBIAR EN PRODUCCIÓN
JWT_SECRET=your-super-secret-key-change-in-production

# NOTA: Las API Keys ahora se guardan en SQL Server (tabla evalImagen.Dispositivo)
# Ya NO es necesario configurar VALID_API_KEYS aquí
# Las apiKeys se gestionan desde la base de datos

# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:3000

# Backend Base URL (para QR Codes de activación)
# URL base del backend (para QR Codes)
BACKEND_BASE_URL=http://localhost:3001/api/
# En producción usar:
# BACKEND_BASE_URL=https://luzsombra-backend.azurewebsites.net/api/

# Development
NODE_ENV=development

# Alert Scheduler Configuration
# Habilitar/deshabilitar el scheduler automático de alertas
# true = habilitado (default), false = deshabilitado
ENABLE_ALERT_SCHEDULER=true
# Nota: El scheduler ejecuta:
# - Consolidación: Diariamente a las 8:00 AM (hora de Santiago)
# - Envío: Cada hora para procesar mensajes pendientes